services:
  db:
    image: postgres:16
    container_name: summarizer_postgres
    restart: always
    ports:
      - '${DB_PORT}:${DB_PORT}'
    volumes:
      - pgdata:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  pgdata:
